import React from "react";
import { setCookie } from "../util/settingSessions";
const useRefreshToken = () => {
  // const refresh = async ()=>{
  //   const response = await
  // }

  return <div>useRefreshToken</div>;
};

export default useRefreshToken;
