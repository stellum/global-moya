import styled from "styled-components";
import { colors, fontWeight, fontSize } from "../theme";

export const ErrorMsgP = styled.p`
  padding: 200px 30px;
  text-align: center;
  color: ${colors.black};
  font-size: ${fontSize.FontSize24};
  font-weight: ${fontWeight.FontWeight600};
`;
