import React from "react";
import { FilterWrap } from "@styles/filterStyle/filterStyle";
const FilterTypeModal = ({ children }) => {
  return <FilterWrap>{children}</FilterWrap>;
};

export default FilterTypeModal;
