import React from "react";
import { DefaultContainer } from "@styles/common/container";
const ErrorMsg = ({ errorMsg }) => {
  return <DefaultContainer>{errorMsg}</DefaultContainer>;
};

export default ErrorMsg;
